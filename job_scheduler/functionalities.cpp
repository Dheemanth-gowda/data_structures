#include <bits/stdc++.h>
using namespace std;
#include "classes.cpp"

void Isps::setStatus(vector<int> statusArray)
{
    for (int i = 0; i < statusArray.size(); i++)
    {
        company[i]
    }
}

void Isps::restartServers()
{
    for (int i = 0; i < isps.size(); i++)
    {
        isps[company[i]] = 1;
    }
}
